export type EventoType = {
    id: string
    titulo: string
    ativo: boolean
    estilo?: string
    logo: string
    chamada: string
    fundo: string
    inscricoesAbertas?: boolean
    inscritos?: any[]
}